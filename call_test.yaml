---
- hosts: localhost
  connection: no
  gather_facts: no
  tasks:
  - name: print call msg
    debug:
      msg: "{{ entry.hostname }}"
    loop: "{{ entries }}"
    loop_control:
      loop_var: entry

